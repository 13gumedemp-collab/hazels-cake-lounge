import Placeholder from "@/components/Placeholder";
export default function Page() {
  return <Placeholder title="Reminder Pipeline" note="Scheduled, sent and needs-attention reminders across all customers, with manual trigger and retry." />;
}
